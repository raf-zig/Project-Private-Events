<h1>Event`s description</h1>
<p><%=@event.description %></p>

<%= form_with(url: "/event_attendances/", method: :post) do %>
  <%= hidden_field_tag :id, @event.id %>
  <%= submit_tag ("I want to be a participant of this event") %>
<% end %>

